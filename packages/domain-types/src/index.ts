// Placeholder: i tipi generati da `pnpm gen:types` (supabase gen types typescript --local)
// verranno esportati come `database.ts` a partire dalla milestone M1, quando esistono le
// prime tabelle. Non scrivere qui tipi a mano: solo output generato.
export {};
