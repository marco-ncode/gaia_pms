// Placeholder: wrapper/job lato TypeScript per le Edge Functions in supabase/functions/api-v1.
// La superficie pubblica /v1 vera e propria (router Hono, auth, scopes, rate limit) vive in
// supabase/functions/api-v1 e viene implementata a partire da M5 (vedi CLAUDE.md §9).
export {};
